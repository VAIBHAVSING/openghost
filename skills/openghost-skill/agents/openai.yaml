interface:
  display_name: "OpenGhost"
  short_description: "Local authorized web and API assessment"
  default_prompt: "Use $openghost-skill to conduct an authorized, scoped web or API assessment with bounded local tooling, compact cached context, evidence-backed findings, explicit coverage, and a validated report."
