interface:
  display_name: "OpenGhost Skill"
  short_description: "Run an authorized web app pentest from a URL and auth context."
  default_prompt: "Use $openghost-skill to run a scoped web application pentest against the provided URL and authentication context, collecting evidence-backed findings and a final report."
